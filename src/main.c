#include <stdio.h>

#define PROGRAM "Rouge Game"

int init();

int main(int *argc, char **argv) {

    init();

    while(1) {
        // Game loop
    }

    return 0;
}

int init() {
    // Game Config Initialize
}